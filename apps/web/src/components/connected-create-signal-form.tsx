'use client';

import { CreateSignalForm, CreateSignalFormProps } from '@hypha-platform/epics';

export function ConnectedCreateSignalForm(props: CreateSignalFormProps) {
  return <CreateSignalForm {...props} />;
}
