import { PATH_ROOT } from '../constants';

export const PATH_SEGMENT_LANG = ':lang';

export const PATH_LANG = `${PATH_ROOT}${PATH_SEGMENT_LANG}`;

export const NETWORK_HYPHA_EARTH_URL = 'https://network.hypha.earth';
