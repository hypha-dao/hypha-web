{
  "name": "hypha-api",
  "version": "1.0.0",
  "description": "",
  "main": "dist/server.js",
  "type": "module",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "scripts": {
    "codegen": "openapi --input docs/v1/openapi.yaml --output src/types/v1/generated --client fetch",
    "dev": "tsx src/server.ts",
    "prebuild": "pnpm codegen",
    "build": "tsc",
    "postbuild": "esbuild src/server.ts --bundle --minify --platform=node --external:yamljs --external:fs --outfile=dist/server.js --external:@fastify/swagger --external:@fastify/swagger-ui",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "@fastify/env": "^5.0.3",
    "@fastify/swagger": "^9.5.1",
    "@fastify/swagger-ui": "^5.2.3",
    "fastify": "^5.4.0",
    "yamljs": "^0.3.0"
  },
  "devDependencies": {
    "@hypha-platform/config-typescript": "workspace:*",
    "@types/yamljs": "^0.2.34",
    "openapi-typescript-codegen": "^0.29.0"
  }
}
