export enum DocumentState {
  DRAFT = 'draft',
  DISCUSSION = 'discussion',
  PROPOSAL = 'proposal',
}

export enum ProposalState {
  ACCEPTED = 'accepted',
  REJECTED = 'rejected',
}

export enum Vote {
  YES = 'yes',
  NO = 'no',
  ABSTAIN = 'abstain',
}
