/** Accent outline pills — border + label use theme accent tokens. */
export const SIGNAL_TAG_BADGE_CLASS =
  'shrink-0 max-w-[8.5rem] overflow-hidden text-ellipsis whitespace-nowrap rounded-full font-medium shadow-none ring-0';

/** Overflow counter (+N) when more tags exist than we show inline. */
export const SIGNAL_TAG_OVERFLOW_BADGE_CLASS =
  'shrink-0 whitespace-nowrap rounded-full border-border/60 bg-transparent text-muted-foreground';
