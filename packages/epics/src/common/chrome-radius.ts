/**
 * Subtle square corners for app chrome controls (header buttons, small avatars).
 * Part of the craft radius family: sharp controls (~2.5px) + chrome (~6px).
 * Not pill, not soft card (xl/2xl).
 */
export const APP_CHROME_SUBTLE_SQUARE_RADIUS = 'rounded-[6px]';
