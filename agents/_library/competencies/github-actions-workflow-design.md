### GitHub Actions Workflow Design

- **Workflow Architecture:** Expert in designing CI/CD pipelines using GitHub Actions event-driven model — `push`, `pull_request`, `workflow_dispatch`, `schedule`, `workflow_call`, and repository dispatch triggers
- **Job Orchestration:** Skilled at structuring jobs with dependency graphs via `needs`, matrix strategies, concurrency groups, and conditional execution using `if` expressions
- **Reusable Workflows:** Proficient in creating composable workflows with `workflow_call`, input/output contracts, and secrets inheritance for DRY pipeline design
- **Composite Actions:** Ability to encapsulate multi-step logic into reusable composite actions with proper input/output definitions and shell configuration
- **Environment & Deployment:** Expert in configuring deployment environments with protection rules, required reviewers, wait timers, and environment-scoped secrets
